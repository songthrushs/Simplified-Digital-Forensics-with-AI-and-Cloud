This code generates a simple UI. With a single click, it locates jpg and png files in a directory. Upon clicking an upload button, the program stores the detected files in AWS S3 storage. Subsequently, it connects to the Amazon Recognition service and retrieves the classification of pictures and display them on the UI based on the content moderation criteria, typically within seconds.
